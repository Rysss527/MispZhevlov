#include "UITheme.h"

namespace UITheme {
    const sf::Color PRIMARY_TEXT = sf::Color::White;
    const sf::Color SECONDARY_TEXT = sf::Color(200, 200, 200);
    const sf::Color ACCENT_TEXT = sf::Color(255, 215, 0);
    const sf::Color SUCCESS_COLOR = sf::Color(34, 139, 34);
    const sf::Color WARNING_COLOR = sf::Color(255, 140, 0);
    const sf::Color ERROR_COLOR = sf::Color(220, 20, 60);
    const sf::Color INFO_COLOR = sf::Color(70, 130, 180);
    
    const sf::Color BUTTON_PRIMARY = sf::Color(50, 150, 50);
    const sf::Color BUTTON_PRIMARY_HOVER = sf::Color(70, 170, 70);
    const sf::Color BUTTON_SECONDARY = sf::Color(100, 100, 100);
    const sf::Color BUTTON_SECONDARY_HOVER = sf::Color(120, 120, 120);
    const sf::Color BUTTON_DISABLED = sf::Color(80, 80, 80);
    
    const sf::Color UI_BACKGROUND = sf::Color(0, 0, 0, 160);
    const sf::Color UI_PANEL = sf::Color(20, 20, 20, 200);
    const sf::Color UI_BORDER = sf::Color(100, 100, 100, 180);
    const sf::Color UI_BORDER_HIGHLIGHT = sf::Color(255, 215, 0, 220);
    
    const sf::Color TEXT_OUTLINE = sf::Color::Black;
    const sf::Color SHADOW_COLOR = sf::Color(0, 0, 0, 128);
    
    const sf::Color GOLD_COLOR = sf::Color(255, 215, 0);
    const sf::Color SUN_COLOR = sf::Color(255, 255, 100);
    const sf::Color HEALTH_FULL = sf::Color(0, 255, 0);
    const sf::Color HEALTH_MEDIUM = sf::Color(255, 255, 0);
    const sf::Color HEALTH_LOW = sf::Color(255, 100, 100);
    
    const int TITLE_SIZE = 48;
    const int SUBTITLE_SIZE = 36;
    const int BODY_SIZE = 24;
    const int BUTTON_SIZE = 28;
    const int SMALL_SIZE = 18;
    const int CAPTION_SIZE = 14;
    const int LARGE_SIZE = 72;
    
    const float OUTLINE_THICKNESS = 2.0f;
    const sf::Vector2f SHADOW_OFFSET = sf::Vector2f(2.0f, 2.0f);
}
